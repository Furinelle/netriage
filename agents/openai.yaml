interface:
  display_name: "netriage (VPS TCP Tuning)"
  short_description: "Evidence-based VPS TCP tuning with BDP and policer tests"
  default_prompt: "Use $netriage to inspect my VPS path, derive candidates, and recommend verified TCP tuning before any changes."
