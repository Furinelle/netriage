interface:
  display_name: "netriage (VPS TCP Tuning)"
  short_description: "Evidence-based VPS TCP tuning: inspect, test, recommend; apply only after approval"
  default_prompt: "Use $netriage to inspect and tune my relay or landing VPS networking safely."
policy:
  allow_implicit_invocation: true
